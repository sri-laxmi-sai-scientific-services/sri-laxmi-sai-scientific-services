const c=window.SLS_CONFIG||{}; let token='';
const $=id=>document.getElementById(id);
$('login').onclick=async()=>{const s=$('loginStatus');s.textContent='Checking…'; if(!c.SUPABASE_URL||!c.SUPABASE_ANON_KEY){s.textContent='Add Supabase URL/key to config.js first.';return;} const r=await fetch(`${c.SUPABASE_URL}/auth/v1/token?grant_type=password`,{method:'POST',headers:{apikey:c.SUPABASE_ANON_KEY,'Content-Type':'application/json'},body:JSON.stringify({email:$('email').value,password:$('password').value})}); const j=await r.json(); if(!r.ok){s.textContent=j.msg||j.error_description||'Login failed';return;} token=j.access_token;s.textContent='Logged in.';$('panel').style.display='block';load();};
async function load(){const h={apikey:c.SUPABASE_ANON_KEY,Authorization:`Bearer ${token}`};const r=await fetch(`${c.SUPABASE_URL}/rest/v1/enquiries?select=*&order=created_at.desc`,{headers:h});const rows=await r.json();const tb=document.querySelector('#table tbody');tb.innerHTML='';(rows||[]).forEach(x=>{const tr=document.createElement('tr');tr.innerHTML=`<td style="padding:12px;border-top:1px solid #ddd">${new Date(x.created_at).toLocaleString()}</td><td style="padding:12px;border-top:1px solid #ddd">${esc(x.type)}</td><td style="padding:12px;border-top:1px solid #ddd"><b>${esc(x.name)}</b><br>${esc(x.company||'')}</td><td style="padding:12px;border-top:1px solid #ddd">${esc(x.phone)}</td><td style="padding:12px;border-top:1px solid #ddd">${esc(x.subject)}</td><td style="padding:12px;border-top:1px solid #ddd">${esc(x.location)}</td><td style="padding:12px;border-top:1px solid #ddd">${esc(x.message)}</td>`;tb.appendChild(tr)})}
function esc(s){return String(s??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]))}$('refresh').onclick=load;
